# caddy-porkbun

A build of Caddy with the Porkbun DNS module included.

## Usage

Pull `git.milkshake.services/floatingmilkshake/caddy-porkbun:latest`, or you can swap `latest` with a specific version of Caddy, like `git.milkshake.services/floatingmilkshake/caddy-porkbun:2.11.3`.
