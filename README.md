# caddy-porkbun

A build of Caddy with the Porkbun DNS module included. Pull `ghcr.io/floatingmilkshake/caddy-porkbun:latest`, or you can swap `latest` with a specific version of Caddy, like `ghcr.io/floatingmilkshake/caddy-porkbun:2.11.3`.
